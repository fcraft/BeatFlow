[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    db_integration: tests that require real PostgreSQL and are run separately (asyncio.run() conflict)
filterwarnings =
    ignore::pytest.PytestUnknownMarkWarning
